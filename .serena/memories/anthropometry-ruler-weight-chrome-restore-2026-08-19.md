# Height/weight original chrome restored (2026-08-19)

Kept 3-column layout (no overlap). Height: T-caps, 3px primary rule, large `rounded-2xl` card with XL number + `см`. Weight: original double-layer pill (shadow ellipse + thick border, `text-lg` `px-7`). Still HTML siblings, not drawn on the SVG body.
