# Список клієнтів — тон балансу (2026-08-18)

Картка в `clients-view.tsx`:
- Текст «ОПЛАТА» прибрано → кружечок `size-2 rounded-full`.
- Ліва смужка `border-l-[3px]` завжди є і того ж кольору, що кружечок.
- `sessionBalance > 1` → emerald-600 (зелений)
- `=== 1` → amber-400 (жовтий)
- `<= 0` → primary (червоний)
- Текст «Залишок …» у `span.min-w-0 truncate`, кружечок `shrink-0`, кнопки телефону/чату лишаються `shrink-0` — рядок більше не залазить під іконки.
