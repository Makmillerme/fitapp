# Bottom nav press feedback (2026-07-27)

NavItem press: fixed size-8 slot (no scale). Only Lucide icon scales to 0.94 with 150ms ease. Slot bg changes on group-active (muted or primary/15). NavSlot flex-1 prevents horizontal drift. FAB uses scale 0.96 + same easing.
