# Anthropometry 3-column layout (2026-08-19)

Height ruler is HTML on the left (`w-11`), skeleton SVG is `flex-1` centered (`FIGURE_TX = -132`, viewBox cropped to 970 so feet aren't under the old weight pill), zone buttons are a fixed `w-14` column with `truncate` (e.g. Передпліччя). Weight is an HTML pill centered under the figure. No overlays covering the body.
