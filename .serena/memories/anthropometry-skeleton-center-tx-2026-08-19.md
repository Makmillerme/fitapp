# Skeleton translate centered (2026-08-19)

Figure group `translateX` is `-140` (was `-80` with ruler / `-132` without). Body center ~512 in a 760 viewBox sits near the card midpoint so the height ruler does not leave a large empty gap on the left.
