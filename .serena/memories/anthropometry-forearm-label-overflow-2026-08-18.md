# Anthropometry zone button overflow (2026-08-18)

Right-column width is `w-28` (was `4.75rem`). Zone buttons are `w-full` with `px-2 py-1.5`.
Labels are no longer `uppercase tracking-wide` — that made «Передпліччя» overflow the active pill. Labels now `text-[10px] leading-tight` in sentence case.
