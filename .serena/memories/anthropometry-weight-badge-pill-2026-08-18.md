# Anthropometry weight badge polish (2026-08-18)

Weight under the skeleton is a pill (`rect` 320×80, rx 40), not a flat ellipse. Text `fontSize 36`, `dominantBaseline=middle` at y=1024 so the number sits centered with padding. Subtle shadow rect underneath.
