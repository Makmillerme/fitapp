## Chrome bottom + soft scrim + smooth carousel — 2026-07-30

1. Chrome mid-photo: fixed top left it mid-square; now continuous lerp top → bottom of photo (no left/width jump).
2. Scrim hard edges: taller band + mask-image fade + softer multi-stop gradient over backdrop-blur.
3. Carousel: removed snap-mandatory/snap-center; free pan-x scroll + soft smooth settle to nearest page after scroll end.
