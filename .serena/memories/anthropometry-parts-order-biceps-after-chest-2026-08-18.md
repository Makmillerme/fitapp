# Anthropometry list order (2026-08-18)

Right-side measurement buttons in `BodyMeasurementsCard` (`PARTS`) and painter `GROUPS`:
Шия → Груди → Біцепс → Талія → Таз → Стегно → Ікра.
Biceps sits under chest so the column follows the skeleton top-to-bottom (arms at chest height, not after hips).
